# Paperthin

Paperthin is an external skill dependency pinned in `harness/paperthin.lock`.
This harness retains only its short `sip` and fresh-context `shower` review
steps for code, configuration, and user-visible behavior changes.

Upgrade the pin explicitly and run `npm run harness:check` afterward.

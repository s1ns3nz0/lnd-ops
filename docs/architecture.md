# Harness architecture

`AGENTS.md` is the short operating guide. `harness.config.json` is the
machine-readable policy. `scripts/` provides bootstrap and configuration
checks. `harness/` contains the pinned external-skill references. Project code
and its tests are the durable record of work.
